<tr>
    <td class="detail">
        <%= progress_link(hq_site.name.to_s, hq_site_path(hq_site)) %>
    </td>
    <td class="command">
        <%= link_to 'Edit', edit_hq_site_path(hq_site) %>
    </td>
		<td class="command">
        <%= link_to 'Destroy', hq_site, :confirm => 'Are you sure?', :method => :delete %>
    </td>
</tr>
